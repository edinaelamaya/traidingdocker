version: "3.9"

services:

  docker-edinael.com:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_ENV: local
    volumes:
      - ./src:/code
    ports:
      - 9500:80
    user: root
